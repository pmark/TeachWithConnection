---
interface Props {
  eyebrow?: string;
  title?: string;
  description?: string;
  background?: "white" | "surface";
  narrow?: boolean;
}

const {
  eyebrow,
  title,
  description,
  background = "white",
  narrow = false,
} = Astro.props;
---

<section class:list={["py-16 sm:py-16", background === "surface" && "bg-paper"]}>
  <div class:list={["mx-auto px-4 sm:px-6", narrow ? "max-w-3xl" : "max-w-6xl"]}>
    {(eyebrow || title || description) && (
      <div class="mb-10 max-w-3xl">
        {eyebrow && (
          <p class="mb-3 text-sm font-semibold uppercase tracking-wide text-brand-text">
            {eyebrow}
          </p>
        )}
        {title && (
          <h2 class="font-serif text-3xl leading-tight text-charcoal sm:text-4xl">
            {title}
          </h2>
        )}
        {description && (
          <p class="mt-4 text-lg leading-8 text-slate">
            {description}
          </p>
        )}
      </div>
    )}
    <slot />
  </div>
</section>
